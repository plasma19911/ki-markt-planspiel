const arr=v=>Array.isArray(v)?v:[];
const num=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
const key=v=>String(v?.symbol||v||'').toUpperCase().trim();
const clamp=(v,a,b)=>Math.min(b,Math.max(a,num(v)));
const brokerExact=c=>c?.brokerVerified===true&&String(c?.assetClass||c?.type||'EQUITY').toUpperCase()==='EQUITY'&&String(c?.brokerMatchMode||'').toUpperCase()==='EXACT_NORMALIZED_NAME'&&/Trade Republic/i.test(String(c?.brokerVerificationSource||''))&&/^[A-Z]{2}[A-Z0-9]{9}[0-9]$/.test(String(c?.isin||''));
const hardReason=s=>/TRADE-REPUBLIC-BLOCK|TARGET-VENUE|HARD[- ]?EVENT|NEWS-SHOCK|STALE QUOTE|BAD QUOTE|FX[- ]?SAFETY|REENTRY|SUSPEND|HALT|DELIST|MARKET CLOSED/i.test(String(s||''));
function promptCandidates(input){for(const m of arr(input?.messages)){const t=String(m?.content||''),a=t.indexOf('Kandidaten='),b=t.indexOf(' Gehalten=',a+11);if(a<0||b<0)continue;try{const x=JSON.parse(t.slice(a+11,b).trim());if(Array.isArray(x))return x}catch{}}return[]}
function parse(r){const raw=String(r?.response||r?.result?.response||''),a=raw.indexOf('{'),b=raw.lastIndexOf('}');if(a<0||b<=a)return null;try{const j=JSON.parse(raw.slice(a,b+1));return Array.isArray(j?.actions)?j:null}catch{return null}}
function encode(r,p){const raw=JSON.stringify(p);if(r?.result&&typeof r.result==='object'&&'response'in r.result)return{...r,result:{...r.result,response:raw}};return{...r,response:raw}}
function isPlan(input){return arr(input?.messages).some(m=>String(m?.content||'').includes('Kandidaten=')&&String(m?.content||'').includes(' Gehalten='))}
function momentum(c={}){return{m5:num(c?.momentum5Pct,c?.momentum5??c?.intraday5m),m20:num(c?.momentum20Pct,c?.momentum20??c?.intraday20m),acc:num(c?.acceleration5Pct,c?.momentumAcceleration5??c?.momentum_acceleration5)}}
function opportunity(c={}){const s=num(c?.daytradeLiveScore,c?.decisionScore??c?.score,0),m=momentum(c),f30=num(c?.forward30Pct,c?.expected30mPct,0),f60=num(c?.forward60Pct,c?.expected60mPct,0),quality=num(c?.entryQualityScore,c?.timingQualityScore,0);return s+clamp(m.m5*2,-3,3)+clamp(m.m20*1.2,-3,3)+clamp(m.acc*1.5,-2,2)+clamp(f30*1.5,-3,4)+clamp(f60*1.2,-3,5)+clamp((quality-50)/20,-1.5,1.5)}
function heldScore(p={},c={}){const stable=num(c?.daytradeLiveScore,c?.decisionScore??p?.decisionScore??p?.score,50),raw=num(c?.rawDecisionScore,p?.rawDecisionScore,stable);return raw<stable-8?Math.max(raw+6,stable-10):stable}
function ageMin(p={},now=Date.now()){const t=Date.parse(String(p?.opened_at??p?.openedAt??''));return Number.isFinite(t)?Math.max(0,(now-t)/60000):9999}
export function enforceProfitOpportunityV305(plan,state={},input=null,now=Date.now()){
 if(!plan||!Array.isArray(plan.actions))return{plan,counters:{}};
 const positions=arr(state?.positions),held=new Set(positions.map(key)),prompt=promptCandidates(input),stateC=arr(state?.candidates),cmap=new Map([...stateC,...prompt].map(c=>[key(c),c]));
 const actions=plan.actions.map(a=>({...a})),idx=new Map();actions.forEach((a,i)=>{if(key(a)&&!idx.has(key(a)))idx.set(key(a),i)});
 const actionFor=s=>{const i=idx.get(s);return i===undefined?null:actions[i]};
 const candidateRows=prompt.filter(c=>!held.has(key(c))&&brokerExact(c)).map(c=>{const a=actionFor(key(c))||{};return{c,s:key(c),a,score:num(c?.daytradeLiveScore,c?.decisionScore??c?.score,0),opp:opportunity(c),m:momentum(c),hard:hardReason(a?.reason)}}).sort((a,b)=>b.opp-a.opp);
 const blocked=candidateRows.filter(x=>String(x.a?.action||'HOLD').toUpperCase()!=='BUY').slice(0,12).map(x=>({symbol:x.s,score:+x.score.toFixed(1),opportunity:+x.opp.toFixed(2),blockedBy:String(x.a?.reason||'AI/Guard HOLD').slice(0,220),hard:x.hard}));
 let starterBuys=0,rotations=0,hysteresisBypasses=0;
 const best=candidateRows.find(x=>x.score>=66&&x.m.m5>=-.05&&x.m.m20>=-.05&&!x.hard)||null;
 if(best&&positions.length<4){const a=actionFor(best.s)||{},act=String(a?.action||'HOLD').toUpperCase();if(act!=='BUY'&&!hardReason(a?.reason)){const buy={...a,symbol:best.s,name:best.c?.name,action:'BUY',allocation_pct:Math.min(10,Math.max(6,6+(best.score-66)*.5)),confidence:clamp(.68+(best.score-66)*.01,.68,.86),profitOpportunityV305:true,starterV305:true,reason:`V30.5 PROFIT-OPPORTUNITY STARTER: ${best.s} ist exakt Trade-Republic-verifiziert und gehoert mit ${best.score.toFixed(1)}/100 zu den staerksten aktuellen Chancen. Weicher HOLD-Block wird nicht als Vollsperre behandelt; kleiner Starter statt MISSED_SAFE_MOVE.`};const i=idx.get(best.s);if(i===undefined){idx.set(best.s,actions.length);actions.push(buy)}else actions[i]=buy;starterBuys++}}
 if(best&&positions.length>=4&&!actions.some(a=>a?.relativeRotationV304&&String(a?.action).toUpperCase()==='SELL')){
  const heldRows=positions.map(p=>{const s=key(p),c=cmap.get(s)||{},stable=num(c?.daytradeLiveScore,c?.decisionScore??p?.decisionScore??p?.score,50),eff=heldScore(p,c),opp=opportunity({...c,decisionScore:eff}),legacy=!brokerExact(c);return{p,c,s,stable,eff,opp,legacy,age:ageMin(p,now)}}).sort((a,b)=>a.opp-b.opp);
  const weak=heldRows[0];if(weak){const netGap=best.opp-weak.opp,required=weak.legacy?3.5:6;if(netGap>=required&&weak.age>=(weak.legacy?5:15)){if(weak.eff<weak.stable-3)hysteresisBypasses++;const wi=idx.get(weak.s),bi=idx.get(best.s),sell={...(wi===undefined?{}:actions[wi]),symbol:weak.s,action:'SELL',allocation_pct:0,confidence:.83,profitOpportunityV305:true,reason:`V30.5 NET-ROTATION: ${weak.s} Opportunity ${weak.opp.toFixed(2)} gegen ${best.s} ${best.opp.toFixed(2)}; Netto-Vorsprung ${netGap.toFixed(2)}. Rohscore-Verschlechterung wird gegen zu traege Score-Hysterese beruecksichtigt.`},buy={...(bi===undefined?{}:actions[bi]),symbol:best.s,name:best.c?.name,action:'BUY',allocation_pct:25,confidence:.82,profitOpportunityV305:true,reason:`V30.5 NET-ROTATION BUY: ${best.s} ersetzt ${weak.s}; hoehere aktuelle 30/60-Minuten-Chance nach Momentum-/Forward-/Kostenheuristik, max. 25% Depotgewicht.`};if(wi===undefined){idx.set(weak.s,actions.length);actions.push(sell)}else actions[wi]=sell;if(bi===undefined){idx.set(best.s,actions.length);actions.push(buy)}else actions[bi]=buy;rotations++}}
 }
 plan.actions=actions;plan.summary=`${String(plan.summary||'').slice(0,130)} · V30.5 Profit-Opportunity: ${starterBuys} Starter · ${rotations} Netto-Rotation · ${blocked.length} starke HOLD-Blockaden protokolliert.`;
 return{plan,counters:{starterBuys,rotations,hysteresisBypasses,blockedStrongCandidates:blocked.length},blocked};
}
export class ProfitOpportunityGuardV305{constructor(inner,{getState,now}={}){this.inner=inner;this.getState=getState;this.now=now;this.latest=null}async run(model,input){const legacy=input===undefined&&model&&typeof model==='object',payload=legacy?model:input,state=typeof this.getState==='function'?(this.getState()||{}):{},r=legacy?await this.inner.run(payload):await this.inner.run(model,payload);if(!isPlan(payload))return r;const p=parse(r);if(!p)return r;const out=enforceProfitOpportunityV305(p,state,payload,typeof this.now==='function'?this.now():Date.now());this.latest=out;return encode(r,out.plan)}status(){return{enabled:true,version:30.5,mode:'profit-opportunity-controller',starterMinScore:66,starterPct:[6,10],normalNetRotationGap:6,legacyNetRotationGap:3.5,maxOpenPositions:4,maxSinglePositionPct:25,hardSafetyNeverOverridden:true,latest:this.latest?.counters||null,blockedStrongCandidates:this.latest?.blocked||[]}}}
