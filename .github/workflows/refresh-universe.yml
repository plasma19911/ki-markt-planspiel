name: Refresh ZERO-target Universe

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/refresh_universe.py"
      - "scripts/refresh_etfs.py"
      - ".github/workflows/refresh-universe.yml"
  schedule:
    - cron: "17 3 * * *"

permissions:
  contents: write

concurrency:
  group: zero-target-universe-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install yfinance
        run: python -m pip install "yfinance>=1.5,<2"
      - name: Refresh liquid ZERO-target stocks
        run: python scripts/refresh_universe.py
      - name: Refresh normal ZERO-target ETF candidates
        run: python scripts/refresh_etfs.py
      - name: Commit changed stock and ETF universes
        shell: bash
        run: |
          if git diff --quiet -- public/universe.json src/generated-zero-etfs.js; then
            echo "Stock/ETF universes unchanged."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/universe.json src/generated-zero-etfs.js
          git commit -m "chore: refresh ZERO-target stock and ETF universes"
          git pull --rebase origin main
          git push origin main
