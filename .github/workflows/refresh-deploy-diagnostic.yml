name: Legacy Deploy Diagnostic V27.7 (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired diagnostic
        run: |
          echo "V27.7 is an archived compatibility diagnostic and no longer runs on pushes."
          echo "Current production is verified by the V31.7+ deploy, main-live, live-health and production-stack workflows."
