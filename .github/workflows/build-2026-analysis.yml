name: Build 2026 Analysis

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/build_2026_analysis.py"
      - "src/constants.js"
      - "src/priority-equities.js"
      - "public/universe.json"
      - ".github/workflows/build-2026-analysis.yml"
  schedule:
    - cron: "42 4 * * *"

permissions:
  contents: write

concurrency:
  group: build-2026-analysis
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install analysis dependencies
        run: python -m pip install "yfinance>=1.5,<2" "pandas>=2.2,<3" "numpy>=2,<3"
      - name: Build 2026 perfect hindsight and walk-forward
        run: python scripts/build_2026_analysis.py
      - name: Commit changed analysis
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/analysis-2026.json
          if git diff --cached --quiet; then
            echo "Analysis unchanged."
            exit 0
          fi
          git commit -m "chore: refresh 2026 analysis"
          git pull --rebase origin main
          git push origin main
