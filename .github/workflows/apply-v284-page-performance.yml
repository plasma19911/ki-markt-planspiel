name: Apply V28.4 page performance and score payload (legacy manual)

# Historical migration only. V31 production already contains the resulting UI/runtime
# changes and must never be rewritten on every main push by an old version patcher.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'V28.4 auto-patcher retired. Current production is maintained by the V31 deployment and validation workflows.'
